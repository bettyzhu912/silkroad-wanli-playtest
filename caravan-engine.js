(function (S) {
'use strict';
// GENERATED by tests/tools/bundle-caravan-engine.js from caravan-demo/engine/*.js — do not edit; the ESM files are the source.
// ---- caravan-demo/engine/data.js (sha256 78269080db8075e0) ----
// GENERATED by tools/gen-data.js from py/vendor/p0/authority/*.json (frozen v0.3 data). Do not edit.
const CARGO_DATA = {"designVersion":"DUNHUANG_CARAVAN_LOADING_v0.3","status":"FROZEN_FOR_PROTOTYPE","idPolicy":"Technical serialization keys introduced by handoff, not official future-city goods IDs.","playerAttributePolicy":"Waiting card shows one core attribute; context shows all; primary choice for dual-attribute cargo is not frozen.","attributeLabels":{"SOFT":"柔软","PRESSURE_SENSITIVE":"怕压","FRAGILE":"易损","DURABLE":"耐压","NORMAL":"普通"},"cargo":[{"id":"silk","name":"绢帛","sourceCity":"长安","sourceScope":"NAMED_CITY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["SOFT"],"archetype":"LIGHT_SOFT","constraintWeight":0.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"paper","name":"纸张","sourceCity":"长安","sourceScope":"NAMED_CITY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["PRESSURE_SENSITIVE"],"archetype":"LIGHT_PRESSURE","constraintWeight":2,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"ceramics","name":"陶瓷","sourceCity":"长安","sourceScope":"NAMED_CITY","weight":5,"weightLevel":"HEAVY","weightLabel":"重","attributes":["FRAGILE"],"archetype":"HEAVY_FRAGILE","constraintWeight":3,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"lacquerware","name":"漆器","sourceCity":"长安","sourceScope":"NAMED_CITY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["FRAGILE","PRESSURE_SENSITIVE"],"archetype":"MEDIUM_FRAGILE_PRESSURE","constraintWeight":2.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"hexi_wool","name":"河西毛织","sourceCity":"敦煌","sourceScope":"NAMED_CITY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["SOFT"],"archetype":"MEDIUM_SOFT","constraintWeight":1,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"dried_fruit","name":"干果","sourceCity":"敦煌","sourceScope":"NAMED_CITY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["NORMAL"],"archetype":"NORMAL_BALANCER","constraintWeight":0,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"medicinal_herbs","name":"药材","sourceCity":"敦煌","sourceScope":"NAMED_CITY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["PRESSURE_SENSITIVE"],"archetype":"LIGHT_PRESSURE","constraintWeight":2,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"dye","name":"染料","sourceCity":"敦煌","sourceScope":"NAMED_CITY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["NORMAL"],"archetype":"NORMAL_BALANCER","constraintWeight":0,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"khotan_silk","name":"于阗丝织","sourceCity":"于阗","sourceScope":"NAMED_CITY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["SOFT"],"archetype":"LIGHT_SOFT","constraintWeight":0.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"khotan_jade","name":"于阗玉","sourceCity":"于阗","sourceScope":"NAMED_CITY","weight":5,"weightLevel":"HEAVY","weightLabel":"重","attributes":["DURABLE"],"archetype":"HEAVY_DURABLE","constraintWeight":1,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"fine_jade","name":"精制玉器","sourceCity":"于阗","sourceScope":"NAMED_CITY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["FRAGILE","PRESSURE_SENSITIVE"],"archetype":"MEDIUM_FRAGILE_PRESSURE","constraintWeight":2.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"felt_shoes","name":"毛毡鞋","sourceCity":"于阗","sourceScope":"NAMED_CITY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["SOFT"],"archetype":"LIGHT_SOFT","constraintWeight":0.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"silverware","name":"银器","sourceCity":null,"sourceScope":"FUTURE_CITY_CANDIDATE_MINIGAME_ONLY","weight":5,"weightLevel":"HEAVY","weightLabel":"重","attributes":["DURABLE"],"archetype":"HEAVY_DURABLE","constraintWeight":1,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"turquoise","name":"绿松石","sourceCity":null,"sourceScope":"FUTURE_CITY_CANDIDATE_MINIGAME_ONLY","weight":3,"weightLevel":"MEDIUM","weightLabel":"中","attributes":["FRAGILE","PRESSURE_SENSITIVE"],"archetype":"MEDIUM_FRAGILE_PRESSURE","constraintWeight":2.5,"visualNote":"最小可辨识视觉，体量与重量大体一致"},{"id":"pepper","name":"胡椒","sourceCity":null,"sourceScope":"FUTURE_CITY_CANDIDATE_MINIGAME_ONLY","weight":1,"weightLevel":"LIGHT","weightLabel":"轻","attributes":["NORMAL"],"archetype":"NORMAL_BALANCER","constraintWeight":0,"visualNote":"小型/中小型袋装，不画大麻袋"}]};
const PROTOTYPE_CONFIG = {"designVersion":"DUNHUANG_CARAVAN_LOADING_v0.3","handoffRevision":"FINAL_MERGE_2026-09-09","configurationPolicy":"Supplied initial values unchanged; tunable is not permission to alter first prototype. Null means undefined, not zero or automatic acceptance.","FROZEN_FOR_PROTOTYPE":{"scope":{"phases":["P0","P1"],"b7Integration":false,"formalVisualAssets":false},"worldTime":{"phases":["晨","昼","暮"],"worldDayTicks":3,"workDurationType":"FULL_DAY","timeCostTicks":2,"formalStartPhases":["晨"],"advanceInterface":"advanceTime","noOvernight":true},"bags":{"count":2,"capacityEach":4,"slotOrder":"BOTTOM_TO_TOP","insertion":"LOWEST_EMPTY_SLOT","removal":"TOP_ONLY"},"batchCargoCounts":[{"batchIndex":1,"min":4,"max":4},{"batchIndex":2,"min":5,"max":7},{"batchIndex":3,"min":8,"max":8}],"cargo":{"count":15,"weights":{"LIGHT":1,"MEDIUM":3,"HEAVY":5},"dataFile":"CARGO_DATA.json"},"scoringArchitecture":{"balance":"RELATIVE_TO_BEST_HIGH_QUALITY_FEASIBLE_REFERENCE","passRequiresBothSubscores":true,"richBalanceMin":80,"richStackingMin":80,"highLayoutNonRichTier":"NORMAL","stackingStart":100,"stackingFloor":0,"softAndDurableBonus":0},"generation":{"solveBeforePresent":true,"sameReferenceAllQualityChecks":true,"physicalHardFloor":0.8},"economy":{"baseWage":9,"cashCap":22,"minimumCompletedBatchesForPay":1,"completionRewards":[2,2,3],"qualityRanges":[[0,1],[0,2],[0,3]],"normalTimeoutPreservesValidBatches":true,"partialCargoReward":false,"reputationDelta":0,"tripIncomeTarget":"tripSummary.livelihoodIncome"},"TRIAL":{"cashDelta":0,"timeCostTicks":0,"tripStateDelta":0,"formalWorkHistory":false},"ABORTED":{"cashDelta":0,"timeCostTicks":0,"formalWorkHistory":false,"preserveBatchPayout":false}},"PLAYTEST_TUNABLE":{"roundDurationSeconds":{"A":75,"B":90},"stacking":{"heavyLayerPenalties":[0,5,12,20],"pressureBands":[{"minAbove":0,"maxAbove":1,"penalty":0},{"minAbove":2,"maxAbove":3,"penalty":8},{"minAbove":4,"maxAbove":5,"penalty":18},{"minAbove":6,"maxAbove":8,"penalty":28},{"minAbove":9,"maxAbove":null,"penalty":38}],"fragileLayerPenalties":[0,0,6,12],"fragileAnyHeavyAbovePenalty":10,"softAboveThreshold":8,"softPenalty":5,"maxPenaltyPerCargo":40},"layoutWeights":{"balance":0.5,"stacking":0.5},"pass":{"layoutMin":55,"balanceMin":40,"stackingMin":40},"performance":{"normalLayoutMin":70,"richLayoutMin":85},"referenceQA":{"preferredPhysicalTarget":0.85,"stackingMin":80,"layoutMin":85},"qualityReward":{"status":"INITIAL_PROTOTYPE_MAPPING_CONFIRMED","B1":{"MODEST":0,"NORMAL":0,"RICH":1},"B2":{"MODEST":0,"NORMAL":1,"RICH":2},"B3":{"MODEST":0,"NORMAL":2,"RICH":3}},"difficulty":{"nearOptimalDelta":10,"batches":[{"batchIndex":1,"targetCW":[2,5],"highConstraintMinCW":2,"highConstraintMaxCount":2,"minimumFlexibleCount":1,"weightComplexity":["LOW","MEDIUM"],"nearOptimalRatioTarget":[0.2,0.6]},{"batchIndex":2,"targetCW":[5,10],"highConstraintMinCW":2,"highConstraintTypicalCount":[2,3],"minimumFlexibleCount":1,"weightComplexity":["MEDIUM"],"nearOptimalRatioTarget":[0.08,0.3],"distinctNearOptimalSolutionsMin":2},{"batchIndex":3,"targetCW":[8,13],"highConstraintMinCW":2,"highConstraintSuggestedCount":[2,4],"cw3MaxCount":2,"minimumFlexibleCount":2,"flexibleMaxCW":1,"weightComplexity":["MEDIUM","HIGH"],"nearOptimalRatioTarget":[0.03,0.18],"distinctNearOptimalSolutionsMin":2}],"tooNarrow":{"batches":[2,3],"operator":"AND","nearOptimalRatioLessThan":0.03,"distinctSolutionsLessThan":2},"tooEasy":{"batch":3,"operator":"AND","cwLessThan":6,"nearOptimalRatioGreaterThan":0.3,"ambiguity":"LOW"}},"boundaryQuestionProportion":null,"balanceHintStrength":null},"CONFIRMED_P1_IMPLEMENTATION":{"classification":"Confirmed initial P1 behavior; reference visual dimensions are not permanent frozen pixels.","entryFlow":["CITY","LIVELIHOOD_LIST","READY"],"howToPlayReturnsTo":"READY","labels":{"insert":"放入 ↓","remove":"取出 ↑","submit":"确认装好","full":"已满","returnLivelihood":"返回营生","exitLivelihood":"退出营生"},"waitingArea":{"columns":4,"maxRows":2,"fixedOriginalPositions":true,"reflow":false,"emptySlotDecoration":false,"returnToOriginalPosition":true},"submit":{"onlyVisibleWhenAllLoaded":true,"location":"WAITING_AREA_CENTER","autoSubmit":false},"balanceBar":{"source":"ACTUAL_PHYSICAL_RATIO","greenMin":0.85,"yellowMin":0.8,"track":"RED_YELLOW_GREEN_YELLOW_RED","visualSegmentProportions":[20,15,30,15,20],"numericOrDirectionalText":false,"selectionPreview":false},"timer":{"runsOnlyDuringOperableGameplay":true,"sharedAcrossBatches":true,"pauseDuringEvaluationTransitionAbortConfirmation":true,"acceptedSubmissionFinishesBeforeTimeoutSettlement":true,"remainingTimeReward":false},"settlement":{"globalClose":false,"tierLabels":{"RICH":"出色","NORMAL":"合格","MODEST":"完成","UNCOMPLETED":"未完成"},"fields":["基础工钱","额外工钱","今日所得"],"trialIncomeLabel":"模拟所得","zeroBatchBaseWageDisplay":false,"exits":["返回营生","退出营生"]},"ui":{"backgroundThemeSlot":"NONE","tapFirst":true,"playerSideLabels":false,"referenceParentSize":[390,844],"referenceWidths":[360,430],"referenceModalSize":[360,610,620]}},"UNDEFINED":{"activeTimerArm":null,"trialIndependentDayDuskEntry":null,"emptyBagsBalanceInitialStateAndExactCoordinates":null,"generatorAttemptLimitAndFallback":null,"sameCargoTypeWithinBatch":null,"batch2CountSamplingProbabilities":null,"boundaryQuestionProportion":null,"weightPartitionAmbiguityQuantification":null,"referenceSelectionAndTieBreakDetails":null,"fullStrategyDedupEncoding":null,"batch1And2FlexibleCargoDefinition":null,"softDifficultyTargetAcceptancePolicy":null,"distinctBelowTwoOutsideTooNarrowRuleHandling":null,"readyAndAbortedGlobalCloseRoute":null,"inputQueueOrMaskDuringEvaluation":null,"overallPerformanceTierAggregation":null,"completionStatusEnumBeyondAborted":null,"elapsedTimeStorageAndTelemetryClockConvention":null,"dualAttributeCardPrimaryChoice":null},"DEBUG":{"playerVisible":false,"requiredFields":["cargoWeights","constraintWeight","leftWeight","rightWeight","physicalBalanceRatio","referenceBestPhysicalRatio","BalanceScore","StackingScore","LayoutScore","penaltySources","referenceSolution","nearOptimalRatio","generationAttempts","rejectReasonHistory"],"rejectReasons":["REJECT_PHYSICAL_BALANCE","REJECT_STACKING_QUALITY","REJECT_LAYOUT_QUALITY","REJECT_CONSTRAINT_WEIGHT","REJECT_TOO_EASY","REJECT_TOO_NARROW","REJECT_DUPLICATE_PATTERN"]}};

// ---- caravan-demo/engine/pyrandom.js (sha256 cedfdc8fd6fbedfc) ----
// Bit-exact port of CPython's random.Random for the test sampler: MT19937 with init_by_array seeding from an int,
// getrandbits, _randbelow_with_getrandbits and sample() (pool branch, n <= setsize). Verified against CPython in tests/parity.js.
class PyRandom {
  constructor(seed) { this.mt = new Uint32Array(624); this.mti = 625; this.seed(seed); }
  initGenrand(s) { const mt = this.mt; mt[0] = s >>> 0; for (let i = 1; i < 624; i++) { const prev = mt[i - 1] ^ (mt[i - 1] >>> 30); mt[i] = (Math.imul(1812433253, prev) + i) >>> 0; } this.mti = 624; }
  initByArray(key) {
    const mt = this.mt; this.initGenrand(19650218); let i = 1, j = 0; const n = key.length;
    for (let k = Math.max(624, n); k > 0; k--) { const prev = mt[i - 1] ^ (mt[i - 1] >>> 30); mt[i] = ((mt[i] ^ Math.imul(prev, 1664525)) + key[j] + j) >>> 0; i++; j++; if (i >= 624) { mt[0] = mt[623]; i = 1; } if (j >= n) j = 0; }
    for (let k = 623; k > 0; k--) { const prev = mt[i - 1] ^ (mt[i - 1] >>> 30); mt[i] = ((mt[i] ^ Math.imul(prev, 1566083941)) - i) >>> 0; i++; if (i >= 624) { mt[0] = mt[623]; i = 1; } }
    mt[0] = 0x80000000; this.mti = 624;
  }
  seed(a) { // CPython: abs(int) split into little-endian 32-bit words
    // Safe-integer arithmetic (no BigInt: the main-game container targets Chrome 61 / ES2017). Bit-exact for |a| < 2^53, which covers every seed the generator uses (test group indices 0–499).
    if (!Number.isSafeInteger(a)) throw new Error('seed must be a safe integer'); let v = Math.abs(a); const key = []; if (v === 0) key.push(0); while (v > 0) { key.push(v % 4294967296); v = Math.floor(v / 4294967296); } this.initByArray(key);
  }
  genrandUint32() {
    const mt = this.mt; let y;
    if (this.mti >= 624) { let kk; for (kk = 0; kk < 624 - 397; kk++) { y = (mt[kk] & 0x80000000) | (mt[kk + 1] & 0x7fffffff); mt[kk] = mt[kk + 397] ^ (y >>> 1) ^ ((y & 1) ? 0x9908b0df : 0); } for (; kk < 623; kk++) { y = (mt[kk] & 0x80000000) | (mt[kk + 1] & 0x7fffffff); mt[kk] = mt[kk + (397 - 624)] ^ (y >>> 1) ^ ((y & 1) ? 0x9908b0df : 0); } y = (mt[623] & 0x80000000) | (mt[0] & 0x7fffffff); mt[623] = mt[396] ^ (y >>> 1) ^ ((y & 1) ? 0x9908b0df : 0); this.mti = 0; }
    y = mt[this.mti++]; y ^= y >>> 11; y ^= (y << 7) & 0x9d2c5680; y ^= (y << 15) & 0xefc60000; y ^= y >>> 18; return y >>> 0;
  }
  random() { const a = this.genrandUint32() >>> 5, b = this.genrandUint32() >>> 6; return (a * 67108864.0 + b) * (1.0 / 9007199254740992.0); }
  getrandbits(k) { if (k <= 0) throw new Error('k>0'); if (k <= 32) return this.genrandUint32() >>> (32 - k); throw new Error('getrandbits>32 unsupported'); }
  randbelow(n) { const k = n.toString(2).length; let r = this.getrandbits(k); while (r >= n) r = this.getrandbits(k); return r; }
  sample(population, k) {
    const n = population.length; if (k < 0 || k > n) throw new Error('sample larger than population');
    let setsize = 21; if (k > 5) setsize += Math.pow(4, Math.ceil(Math.log(k * 3) / Math.log(4)));
    if (n > setsize) throw new Error('set-based sample branch not ported');
    const pool = population.slice(), result = new Array(k);
    for (let i = 0; i < k; i++) { const j = this.randbelow(n - i); result[i] = pool[j]; pool[j] = pool[n - i - 1]; }
    return result;
  }
}

// ---- caravan-demo/engine/core.js (sha256 14fc273339c6f93f) ----
// Port of vendor/p0/src/core.py (P0 pure scoring). The Python file remains the authority; tests/parity.js proves equality.

const CONFIG = PROTOTYPE_CONFIG, F = CONFIG.FROZEN_FOR_PROTOTYPE, T = CONFIG.PLAYTEST_TUNABLE;
const CATALOG = Object.fromEntries(CARGO_DATA.cargo.map(c => [c.id, c]));
const ATTR = CARGO_DATA.attributeLabels;
class Cargo { constructor(instance, kind, weight, attributes, archetype, cw) { this.instance = instance; this.kind = kind; this.weight = weight; this.attributes = attributes; this.archetype = archetype; this.cw = cw; Object.freeze(this); } }
function cargo(kind, instance = null) { const c = CATALOG[kind]; if (!c) throw new Error('unknown cargo ' + kind); return new Cargo(instance || kind, kind, c.weight, Object.freeze(c.attributes.slice()), c.archetype, c.constraintWeight); }
function cargoEquals(a, b) { return a.instance === b.instance && a.kind === b.kind && a.weight === b.weight && a.archetype === b.archetype && a.cw === b.cw && a.attributes.length === b.attributes.length && a.attributes.every((x, i) => x === b.attributes[i]); }
function physical_ratio(left, right) { if (left < 0 || right < 0 || !Number.isFinite(left + right)) throw new Error('invalid weight'); const mx = Math.max(left, right); return mx ? Math.min(left, right) / mx : null; }
function classify(balance, stacking) {
  for (const v of [balance, stacking]) if (!(Number.isFinite(v) && v >= 0 && v <= 100)) throw new Error('invalid score');
  const layout = T.layoutWeights.balance * balance + T.layoutWeights.stacking * stacking, p = T.pass;
  const passed = layout >= p.layoutMin && balance >= p.balanceMin && stacking >= p.stackingMin;
  let tier = null;
  if (passed) tier = (layout >= T.performance.richLayoutMin && balance >= F.scoringArchitecture.richBalanceMin && stacking >= F.scoringArchitecture.richStackingMin) ? 'RICH' : layout < T.performance.normalLayoutMin ? 'MODEST' : 'NORMAL';
  return [layout, passed, tier];
}
function item_penalty(c, layer, above) {
  const st = T.stacking, parts = {}; const weight_above = above.reduce((s, x) => s + x.weight, 0);
  if (c.weight === 5) parts.heavyLayer = st.heavyLayerPenalties[layer - 1];
  if (c.attributes.includes('PRESSURE_SENSITIVE') && !c.attributes.includes('DURABLE')) parts.pressure = st.pressureBands.find(b => weight_above >= b.minAbove && (b.maxAbove === null || weight_above <= b.maxAbove)).penalty;
  if (c.attributes.includes('FRAGILE')) { parts.fragileLayer = st.fragileLayerPenalties[layer - 1]; parts.fragileHeavyAbove = above.some(x => x.weight === 5) ? st.fragileAnyHeavyAbovePenalty : 0; }
  if (c.attributes.includes('SOFT')) parts.soft = weight_above >= st.softAboveThreshold ? st.softPenalty : 0;
  const raw = Object.values(parts).reduce((s, v) => s + v, 0);
  return { instance: c.instance, layer, weightAbove: weight_above, components: parts, rawPenalty: raw, penalty: Math.min(st.maxPenaltyPerCargo, raw) };
}
const bagCache = new Map();
function bag_summary(bag) {
  const key = bag.map(c => c.instance + '|' + c.kind).join(',');
  let v = bagCache.get(key); if (v) return v;
  const w = bag.reduce((s, c) => s + c.weight, 0); let p = 0;
  for (let i = 0; i < bag.length; i++) p += item_penalty(bag[i], i + 1, bag.slice(i + 1)).penalty;
  v = [w, p]; if (bagCache.size > 30000) bagCache.clear(); bagCache.set(key, v); return v;
}
function validate_layout(left, right, expected = null) {
  if (left.length > 4 || right.length > 4 || [...left, ...right].some(c => !(c instanceof Cargo))) throw new Error('REJECT_FEASIBILITY');
  const ids = [...left, ...right].map(c => c.instance); if (new Set(ids).size !== ids.length) throw new Error('REJECT_FEASIBILITY');
  if (expected !== null) { const a = ids.slice().sort(), b = expected.map(c => c.instance).sort(); if (a.length !== b.length || a.some((x, i) => x !== b[i])) throw new Error('REJECT_INCOMPLETE_LAYOUT'); }
}
function evaluateLayout(left, right, referenceBestPhysicalRatio, { expected = null, debug = false } = {}) {
  left = Array.from(left); right = Array.from(right); validate_layout(left, right, expected);
  const [lw, lp] = bag_summary(left), [rw, rp] = bag_summary(right); const ratio = physical_ratio(lw, rw);
  if (ratio === null) return { state: 'EMPTY_UNSCORED', physicalBalanceRatio: null, BalanceScore: null, StackingScore: null, LayoutScore: null, passed: false, performanceTier: null };
  if (!Number.isFinite(referenceBestPhysicalRatio) || !(0 < referenceBestPhysicalRatio && referenceBestPhysicalRatio <= 1)) throw new Error('invalid reference denominator');
  const balance = Math.min(100.0, 100 * ratio / referenceBestPhysicalRatio), stacking = Math.max(0, 100 - lp - rp);
  const [layout, passed, tier] = classify(balance, stacking);
  const score = { physicalBalanceRatio: ratio, BalanceScore: balance, StackingScore: stacking, LayoutScore: layout, passed, performanceTier: tier };
  if (!debug) return score;
  return { ...score, leftWeight: lw, rightWeight: rw, cargoWeights: [...left, ...right].map(c => c.weight), constraintWeight: [...left, ...right].reduce((s, c) => s + c.cw, 0), referenceBestPhysicalRatio,
    penaltySources: { left: left.map((c, i) => item_penalty(c, i + 1, left.slice(i + 1))), right: right.map((c, i) => item_penalty(c, i + 1, right.slice(i + 1))) },
    warnings: ratio > referenceBestPhysicalRatio ? ['WARN_PLAYER_EXCEEDS_REFERENCE_PHYSICAL_RATIO'] : [] };
}
// Python tuple/list comparison (lexicographic, element-wise; numbers or strings or nested arrays)
function cmp(a, b) {
  if (Array.isArray(a) && Array.isArray(b)) { const n = Math.min(a.length, b.length); for (let i = 0; i < n; i++) { const c = cmp(a[i], b[i]); if (c) return c; } return a.length - b.length; }
  return a < b ? -1 : a > b ? 1 : 0;
}
const pyMin = (a, b) => cmp(a, b) <= 0 ? a : b;
function canonical_layout(left, right) { const pair = [left.map(c => c.instance), right.map(c => c.instance)]; return pyMin(pair, [pair[1], pair[0]]); }
const sortStr = a => a.slice().sort((x, y) => x < y ? -1 : x > y ? 1 : 0), sortNum = a => a.slice().sort((x, y) => x - y);
function strategy_signature(left, right) {
  const all = [...left, ...right];
  const side = b => [b.reduce((s, c) => s + c.weight, 0), sortStr(b.slice(0, 2).map(c => c.archetype)), sortStr(b.slice(2).map(c => c.archetype))];
  const pair = [side(left), side(right)];
  return [all.length, sortNum(all.map(c => c.weight)), sortStr(all.map(c => c.archetype)), all.filter(c => c.cw >= 2).length,
    all.filter(c => c.archetype === 'HEAVY_DURABLE').length, all.filter(c => c.archetype === 'HEAVY_FRAGILE').length,
    all.filter(c => c.attributes.includes('PRESSURE_SENSITIVE')).length, all.filter(c => c.attributes.includes('SOFT')).length, pyMin(pair, [pair[1], pair[0]])];
}
function serialize_signature(s) { return JSON.stringify(s); }

// ---- caravan-demo/engine/generator.js (sha256 95f666d1f8a3921b) ----
// Port of vendor/p0/src/generator.py (exhaustive solve-before-present generator, two-layer QA, 200-attempt cap).

function* combinations(n, k) { const idx = Array.from({ length: k }, (_, i) => i); if (k > n) return; for (;;) { yield idx.slice(); let i = k - 1; while (i >= 0 && idx[i] === i + n - k) i--; if (i < 0) return; idx[i]++; for (let j = i + 1; j < k; j++) idx[j] = idx[j - 1] + 1; } }
function* permutations(arr) { const n = arr.length, c = new Array(n).fill(0), a = arr.slice(); yield a.slice(); let i = 0; // Python itertools order via index-array algorithm
  const pool = arr, indices = Array.from({ length: n }, (_, k) => k), cycles = Array.from({ length: n }, (_, k) => n - k); if (n === 0) return;
  for (;;) { let found = false; for (let ii = n - 1; ii >= 0; ii--) { cycles[ii]--; if (cycles[ii] === 0) { const t = indices[ii]; indices.splice(ii, 1); indices.push(t); cycles[ii] = n - ii; } else { const j = cycles[ii]; const tmp = indices[ii]; indices[ii] = indices[n - j]; indices[n - j] = tmp; yield indices.map(x => pool[x]); found = true; break; } } if (!found) return; }
}
function* feasible_layouts(items) {
  items = Array.from(items); const n = items.length;
  for (let left_n = Math.max(0, n - 4); left_n <= Math.min(4, n); left_n++) for (const chosen of combinations(n, left_n)) {
    const set = new Set(chosen); const a = items.filter((_, i) => set.has(i)), b = items.filter((_, i) => !set.has(i));
    const right_orders = Array.from(permutations(b));
    for (const left of permutations(a)) for (const right of right_orders) yield [left, right];
  }
}
function first_layer_qa(ratio, stacking, layout) { const r = []; if (ratio < F.generation.physicalHardFloor) r.push('REJECT_PHYSICAL_BALANCE'); if (stacking < T.referenceQA.stackingMin) r.push('REJECT_STACKING_QUALITY'); if (layout < T.referenceQA.layoutMin) r.push('REJECT_LAYOUT_QUALITY'); return r; }
function select_reference(layouts) {
  let best_key = null, reference = null, qualified_count = 0; const failure_counts = {};
  for (const [left, right] of layouts) {
    const lw = bag_summary(left)[0], rw = bag_summary(right)[0], ratio = physical_ratio(lw, rw);
    const score = evaluateLayout(left, right, ratio ? ratio : 1.0); const reasons = first_layer_qa(ratio, score.StackingScore, score.LayoutScore);
    if (reasons.length) { for (const r of reasons) failure_counts[r] = (failure_counts[r] || 0) + 1; continue; }
    qualified_count++;
    const key = [-score.LayoutScore, -ratio, -score.StackingScore, canonical_layout(left, right)];
    if (best_key === null || cmp(key, best_key) < 0) { best_key = key; let L = left, R = right; if (cmp(L.map(c => c.instance), R.map(c => c.instance)) > 0) { L = right; R = left; } reference = [L, R, score]; }
  }
  return [reference, qualified_count, failure_counts];
}
function partition_ambiguity(items) {
  const parts = new Map(), n = items.length;
  for (let k = Math.max(0, n - 4); k <= Math.min(n, 4); k++) for (const a of combinations(n, k)) {
    const set = new Set(a), b = items.map((_, i) => i).filter(i => !set.has(i));
    const ids = [a.map(i => items[i].instance).sort(), b.map(i => items[i].instance).sort()]; const key = JSON.stringify(pyMin(ids, [ids[1], ids[0]]));
    parts.set(key, physical_ratio(a.reduce((s, i) => s + items[i].weight, 0), b.reduce((s, i) => s + items[i].weight, 0)));
  }
  const vals = [...parts.values()]; const best = Math.max(...vals); const count = vals.filter(r => r >= best - .05).length;
  return { bestPhysicalRatio: best, nearBestWeightPartitions: count, weightPartitionAmbiguity: count <= 1 ? 'LOW' : count === 2 ? 'MEDIUM' : 'HIGH' };
}
function composition_qa(batch, items) {
  const reasons = [], allowed = { 1: [4], 2: [5, 6, 7], 3: [8] };
  if (!allowed[batch] || !allowed[batch].includes(items.length) || new Set(items.map(c => c.instance)).size !== items.length) return ['REJECT_FEASIBILITY'];
  if (items.some(c => !CATALOG[c.kind] || !cargoEquals(c, cargo(c.kind, c.instance)))) return ['REJECT_CARGO_POOL'];
  if (items.reduce((s, c) => s + c.cw, 0) > T.difficulty.batches[batch - 1].targetCW[1]) reasons.push('REJECT_CONSTRAINT_WEIGHT');
  const high = items.filter(c => c.cw >= 2).length;
  if (batch === 1 && (high > 2 || high === items.length)) reasons.push('REJECT_HIGH_CONSTRAINT_DENSITY');
  if (batch === 3 && items.filter(c => c.cw === 3).length > 2) reasons.push('REJECT_HIGH_CONSTRAINT_DENSITY');
  return reasons;
}
function difficulty_qa(batch, items, near_ratio, distinct, ambiguity) {
  const reasons = [], warnings = [], cw = items.reduce((s, c) => s + c.cw, 0);
  if ((batch === 2 || batch === 3) && near_ratio < .03 && distinct < 2) reasons.push('REJECT_TOO_NARROW');
  if (batch === 3 && cw < 6 && near_ratio > .30 && ambiguity === 'LOW') reasons.push('REJECT_TOO_EASY');
  const cfg = T.difficulty.batches[batch - 1];
  if (!(cfg.targetCW[0] <= cw && cw <= cfg.targetCW[1])) warnings.push('WARN_CW_TARGET');
  if (!(cfg.nearOptimalRatioTarget[0] <= near_ratio && near_ratio <= cfg.nearOptimalRatioTarget[1])) warnings.push('WARN_NEAR_OPTIMAL_TARGET');
  if (!cfg.weightComplexity.includes(ambiguity)) warnings.push('WARN_WEIGHT_COMPLEXITY');
  const high = items.filter(c => c.cw >= 2).length;
  if (batch === 2 && !(2 <= high && high <= 3)) warnings.push('WARN_HIGH_CONSTRAINT_TARGET');
  if (batch === 3 && !(2 <= high && high <= 4)) warnings.push('WARN_HIGH_CONSTRAINT_TARGET');
  if (batch === 3 && items.filter(c => c.cw <= 1).length < 2) warnings.push('WARN_FLEXIBLE_CARGO_TARGET');
  if ((batch === 2 || batch === 3) && distinct < 2) warnings.push('WARN_DISTINCT_TARGET');
  return [reasons, warnings];
}
const sortedUnique = a => [...new Set(a)].sort((x, y) => x < y ? -1 : x > y ? 1 : 0);
class Board {
  constructor(batch, items, left, right, reference_ratio, metrics) { this.batch = batch; this.items = items; this.left = left; this.right = right; this.reference_ratio = reference_ratio; this.metrics = metrics; }
  evidence() { return { batch: this.batch, cargo: this.items.map(c => ({ instance: c.instance, kind: c.kind })), referenceSolution: { left: this.left.map(c => c.instance), right: this.right.map(c => c.instance) }, referencePhysicalBalanceRatio: this.reference_ratio, ...this.metrics, referenceDebug: evaluateLayout(this.left, this.right, this.reference_ratio, { expected: this.items, debug: true }) }; }
}
function analyze_board(batch, items, recent = []) {
  items = Array.from(items); let reasons = composition_qa(batch, items); if (reasons.length) return [null, reasons];
  const [ref, qcount, fail_counts] = select_reference(feasible_layouts(items));
  if (ref === null) return [null, ['REJECT_REFERENCE_NOT_FOUND', ...Object.keys(fail_counts).sort()]];
  const [left, right, rs] = ref, ratio = rs.physicalBalanceRatio; const values = []; let best = -1;
  for (const [a, b] of feasible_layouts(items)) { const s = evaluateLayout(a, b, ratio); best = Math.max(best, s.LayoutScore); values.push([a, b, s.LayoutScore]); }
  const near = values.filter(([, , l]) => l >= best - T.difficulty.nearOptimalDelta).map(([a, b]) => [a, b]);
  const distinct = new Set(near.map(([a, b]) => serialize_signature(strategy_signature(a, b)))).size;
  const near_ratio = near.length / values.length, ambiguity = partition_ambiguity(items);
  let warnings; [reasons, warnings] = difficulty_qa(batch, items, near_ratio, distinct, ambiguity.weightPartitionAmbiguity);
  const sig = serialize_signature(strategy_signature(left, right));
  for (const prior of recent) {
    if (sig === prior.metrics.decisionPatternSignature) reasons.push('REJECT_DUPLICATE_DECISION_PATTERN');
    const kinds = new Set(items.map(c => c.kind)), pk = new Set(prior.items.map(c => c.kind)); if ([...kinds].some(k => pk.has(k))) warnings.push('WARN_REPEAT_CARGO');
    const arch = new Set(items.map(c => c.archetype)), pa = new Set(prior.items.map(c => c.archetype)); if ([...arch].some(k => pa.has(k))) warnings.push('WARN_REPEAT_ARCHETYPE');
    const w1 = items.map(c => c.weight).sort((x, y) => x - y).join(), w2 = prior.items.map(c => c.weight).sort((x, y) => x - y).join(); if (w1 === w2) warnings.push('WARN_REPEAT_WEIGHT_PATTERN');
  }
  if (reasons.length) return [null, sortedUnique(reasons)];
  const uw = sortedUnique(warnings);
  const metrics = { referenceStackingScore: rs.StackingScore, referenceLayoutScore: rs.LayoutScore, qualifiedReferenceCount: qcount, allFeasibleLayoutCount: values.length, nearOptimalLayoutCount: near.length, nearOptimalRatio: near_ratio, distinctNearOptimalSolutions: distinct, bestFeasibleLayoutScore: best, decisionPatternSignature: sig, constraintWeight: items.reduce((s, c) => s + c.cw, 0), boundary: ratio < T.referenceQA.preferredPhysicalTarget, ...ambiguity, warnings: uw, qaStatus: uw.length ? 'ACCEPT_WITH_WARNING' : 'ACCEPT',
    repeatTelemetry: recent.map(p => { const kinds = new Set(items.map(c => c.kind)), arch = new Set(items.map(c => c.archetype)); return { batch: p.batch, sharedCargoKinds: [...new Set(p.items.map(c => c.kind))].filter(k => kinds.has(k)).length, sharedArchetypes: [...new Set(p.items.map(c => c.archetype))].filter(k => arch.has(k)).length }; }) };
  return [new Board(batch, items, left, right, ratio, metrics), []];
}
function generate_board(batch, sampler, { seed, recent = [] }) {
  if (recent.length > batch - 1 || recent.some(p => p.batch >= batch)) throw new Error('invalid current-run history');
  const counts = {}, attempts = [];
  for (let attempt = 1; attempt <= 200; attempt++) {
    const items = Array.from(sampler(attempt)); const [board, reasons] = analyze_board(batch, items, recent);
    attempts.push({ attempt, cargo: items.map(c => c.kind), rejectReasons: reasons });
    if (board) { Object.assign(board.metrics, { generationAttempts: attempt, seed, rejectReasonHistory: attempts.slice(0, -1), rejectReasonDistribution: { ...counts } }); return [board, { status: board.metrics.qaStatus, attemptCount: attempt, attempts, rejectReasonDistribution: { ...counts } }]; }
    for (const r of reasons) counts[r] = (counts[r] || 0) + 1;
  }
  return [null, { status: 'GENERATION_EXHAUSTED', batch, seed, attemptCount: 200, rejectReasonDistribution: { ...counts }, attempts }];
}

// ---- caravan-demo/engine/stress.js (sha256 28f7e888a13bb1e3) ----
// Port of vendor/p0/tests/stress.py one_run(index): the TEST-ONLY seeded sampler (uniform no-replacement from the 15 kinds).



const factorial = n => { let f = 1; for (let i = 2; i <= n; i++) f *= i; return f; };
function one_run(index) {
  const b2_size = index < 167 ? 5 : index < 333 ? 6 : 7; const recent = [], results = []; const kinds = Object.keys(CATALOG).sort();
  for (const [batch, size] of [[1, 4], [2, b2_size], [3, 8]]) {
    const seed = 202609090000 + index * 10 + batch, rng = new PyRandom(seed);
    const sampler = () => rng.sample(kinds, size).map(k => cargo(k));
    const [board, trace] = generate_board(batch, sampler, { seed, recent });
    if (board === null) { results.push({ run: index, batch, seed, status: 'GENERATION_EXHAUSTED', trace }); break; }
    const ev = board.evidence(); const errors = [];
    const s = evaluateLayout(board.left, board.right, board.reference_ratio, { expected: board.items });
    errors.push(...composition_qa(batch, board.items), ...first_layer_qa(s.physicalBalanceRatio, s.StackingScore, s.LayoutScore), ...difficulty_qa(batch, board.items, ev.nearOptimalRatio, ev.distinctNearOptimalSolutions, ev.weightPartitionAmbiguity)[0]);
    if (s.BalanceScore !== 100 || s.performanceTier === null) errors.push('INVALID_REFERENCE_SCORE_OR_TIER');
    if (ev.referenceLayoutScore !== s.LayoutScore || ev.referenceStackingScore !== s.StackingScore) errors.push('REFERENCE_MISMATCH');
    if (ev.allFeasibleLayoutCount !== factorial(size) * (9 - size)) errors.push('ENUMERATION_COUNT_MISMATCH');
    if (ev.nearOptimalLayoutCount / ev.allFeasibleLayoutCount !== ev.nearOptimalRatio) errors.push('NEAR_COUNT_MISMATCH');
    if (ev.decisionPatternSignature !== serialize_signature(strategy_signature(board.left, board.right))) errors.push('SIGNATURE_MISMATCH');
    if (recent.some(p => ev.decisionPatternSignature === p.metrics.decisionPatternSignature)) errors.push('DUPLICATE_PATTERN_ACCEPTED');
    if (partition_ambiguity(board.items).weightPartitionAmbiguity !== ev.weightPartitionAmbiguity) errors.push('AMBIGUITY_MISMATCH');
    results.push({ run: index, batch, size, seed, status: 'ACCEPTED', validationErrors: errors, board: ev, trace });
    recent.push(board);
  }
  return results;
}

// ---- caravan-demo/engine/session.js (sha256 c0c47d324dce92cc) ----
// Port of vendor/p0/src/session.py (headless three-batch session and mock-only settlement contracts).

function formal_entry(phase) { return { visible: phase !== '暮', enabled: phase === '晨', reason: phase === '昼' ? '今日时间不足' : null }; }
function payout(tiers) {
  if (tiers.length > 3 || tiers.some(t => !['MODEST', 'NORMAL', 'RICH'].includes(t))) throw new Error('only sequential valid batch tiers');
  const base = tiers.length ? F.economy.baseWage : 0;
  const rewards = tiers.map((tier, i) => ({ batchIndex: i + 1, completionReward: F.economy.completionRewards[i], qualityReward: T.qualityReward['B' + (i + 1)][tier] }));
  const extra = rewards.reduce((s, x) => s + x.completionReward + x.qualityReward, 0);
  return { baseWage: base, extraWage: extra, cash: Math.min(F.economy.cashCap, base + extra), rewards };
}
const clone = v => JSON.parse(JSON.stringify(v));
class Session {
  constructor(board_provider, { mode, timerConfig, phase = '晨', jobId = 'P0_MOCK_JOB', runId = 'P0_MOCK_RUN' }) {
    if (!['FORMAL', 'TRIAL'].includes(mode) || ![75, 90].includes(timerConfig)) throw new Error('explicit mode/timer required');
    if (mode === 'FORMAL' && !formal_entry(phase).enabled) throw new Error('formal entry unavailable');
    this.provider = board_provider; this.mode = mode; this.timerConfig = timerConfig; this.remaining = Number(timerConfig); this.elapsed = 0; this.batch = 1;
    this.jobId = jobId; this.runId = runId; this.results = []; this.state = 'GAMEPLAY'; this.pending_timeout = false; this.result = null; this.last_evaluation = null; this._load();
  }
  _load() { this.board = this.provider(this.batch); if (!this.board || this.board.batch !== this.batch) throw new Error('board unavailable'); this.left = []; this.right = []; this.by_id = Object.fromEntries(this.board.items.map(c => [c.instance, c])); this.original_slots = this.board.items.map(c => c.instance); this.waiting = new Set(this.original_slots); }
  _operable() { if (this.state !== 'GAMEPLAY') throw new Error('INPUT_LOCKED'); }
  get can_submit() { return this.state === 'GAMEPLAY' && this.waiting.size === 0; }
  insert(instance, side) { this._operable(); if (!['left', 'right'].includes(side) || !this.waiting.has(instance)) throw new Error('invalid placement'); const bag = this[side]; if (bag.length === 4) throw new Error('已满'); bag.push(this.by_id[instance]); this.waiting.delete(instance); }
  remove(instance, side) { this._operable(); if (!['left', 'right'].includes(side)) throw new Error('invalid side'); const bag = this[side]; if (!bag.length || bag[bag.length - 1].instance !== instance) throw new Error('先取出上层货物'); const c = bag.pop(); this.waiting.add(c.instance); }
  debug() { return { ...evaluateLayout(this.left, this.right, this.board.reference_ratio, { debug: true }), referenceSolution: { left: this.board.left.map(c => c.instance), right: this.board.right.map(c => c.instance) }, nearOptimalRatio: this.board.metrics.nearOptimalRatio, generationAttempts: this.board.metrics.generationAttempts ?? null, rejectReasonHistory: this.board.metrics.rejectReasonHistory || [] }; }
  tick(seconds) { if (!Number.isFinite(seconds) || seconds < 0) throw new Error('invalid clock delta'); if (this.state !== 'GAMEPLAY') return; const used = Math.min(seconds, this.remaining); this.remaining -= used; this.elapsed += used; if (this.remaining === 0) this.timeout_event(); }
  timeout_event() { if (['SETTLEMENT', 'ABORTED'].includes(this.state)) return; if (this.state === 'ABORT_CONFIRM') return; this.remaining = 0; if (this.state === 'EVALUATING') this.pending_timeout = true; else this._settle('TIMEOUT'); }
  submit() { this._operable(); if (!this.can_submit) throw new Error('REJECT_INCOMPLETE_LAYOUT'); this.state = 'EVALUATING'; this.submitted_at = this.elapsed; this.snapshot = [this.left.slice(), this.right.slice()]; }
  resolve_submission() {
    if (this.state !== 'EVALUATING') throw new Error('no accepted submission');
    const [a, b] = this.snapshot; const s = evaluateLayout(a, b, this.board.reference_ratio, { expected: this.board.items }); this.last_evaluation = s;
    if (s.passed) this.results.push({ batchIndex: this.batch, cargoIds: this.board.items.map(c => c.kind), performanceTier: s.performanceTier, balanceScore: s.BalanceScore, stackingScore: s.StackingScore, layoutScore: s.LayoutScore, submittedAtSeconds: this.submitted_at, rewardData: payout([...this.results.map(x => x.performanceTier), s.performanceTier]).rewards.slice(-1)[0] });
    if (this.pending_timeout || this.remaining === 0) this._settle('TIMEOUT'); else if (s.passed && this.batch === 3) this._settle('ALL_BATCHES_COMPLETED'); else if (s.passed) this.state = 'TRANSITION'; else this.state = 'GAMEPLAY';
    return s;
  }
  finish_transition() { if (this.state !== 'TRANSITION') throw new Error('no transition'); this.batch++; this._load(); this.state = 'GAMEPLAY'; }
  request_abort() { this._operable(); this.state = 'ABORT_CONFIRM'; }
  cancel_abort() { if (this.state !== 'ABORT_CONFIRM') throw new Error('no abort confirmation'); this.state = 'GAMEPLAY'; }
  confirm_abort() { if (this.state !== 'ABORT_CONFIRM') throw new Error('no abort confirmation'); this._settle('ABORTED'); }
  _settle(reason) {
    if (this.result !== null) return this.result;
    const aborted = reason === 'ABORTED', formal = this.mode === 'FORMAL' && !aborted; const pay = aborted ? payout([]) : payout(this.results.map(r => r.performanceTier));
    this.state = aborted ? 'ABORTED' : 'SETTLEMENT';
    this.result = { minigameId: 'DUNHUANG_CARAVAN_LOADING', jobId: this.jobId, runId: this.runId, mode: this.mode, completionStatus: aborted ? 'ABORTED' : null, p0TerminationReason: reason, performanceTier: null,
      primaryMetrics: { completedBatchCount: this.results.length, batchResults: clone(this.results) },
      secondaryMetrics: { finalBalanceScores: this.results.map(r => r.balanceScore), finalStackingScores: this.results.map(r => r.stackingScore), finalLayoutScores: this.results.map(r => r.layoutScore) }, bonusMetrics: {},
      durationData: { elapsedSeconds: this.elapsed, remainingSeconds: this.remaining, clock: 'P0_SYNTHETIC_OPERABLE_SECONDS' },
      cashDelta: formal ? pay.cash : 0, timeCostTicks: formal ? 2 : 0, tripStateDelta: 0, formalWorkHistory: formal, reputationDelta: 0,
      simulatedPayout: this.mode === 'TRIAL' && !aborted ? pay.cash : 0, payoutBreakdown: pay };
    return this.result;
  }
  navigate(destination) { if (this.state !== 'SETTLEMENT' || !['LIVELIHOOD_LIST', 'CITY'].includes(destination)) throw new Error('invalid navigation'); return destination; }
}
class MockOuter {
  constructor(active_trip = false) { this.state = { cash: 100, worldTicks: 0, tripSummary: { livelihoodIncome: 0 }, tradeProfit: 17, reputation: 3, reputationTurnover: 11, goods: ['unchanged'], market: { unchanged: true }, workHistory: [] }; this.applied = new Set(); this.active_trip = active_trip; }
  apply(result) {
    if (result.mode === 'TRIAL' || result.completionStatus === 'ABORTED') return; if (this.applied.has(result.runId)) return; this.applied.add(result.runId);
    this.state.cash += result.cashDelta; this.advanceTime(result.timeCostTicks);
    if (this.active_trip) this.state.tripSummary.livelihoodIncome += result.cashDelta;
    if (result.formalWorkHistory) this.state.workHistory.push({ runId: result.runId, cash: result.cashDelta, batchTiers: result.primaryMetrics.batchResults.map(x => x.performanceTier) });
  }
  advanceTime(ticks) { this.state.worldTicks += ticks; }
}

// ---- caravan-demo/engine/game.js (sha256 329af8aaa90b9ee0) ----
// Port of the P1 presentation adapter (P1_PLAYABLE server.py `Game`, deployment copy py/p1_game.py): same states, actions,
// snapshot fields and timings. Board preparation is synchronous (the worker is the background thread).




const uuid = () => (globalThis.crypto && crypto.randomUUID) ? crypto.randomUUID() : 'run-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 10);
const now = () => (typeof performance !== 'undefined' ? performance.now() : Date.now()) / 1000;
class Game {
  constructor(clock = now) { this.clock = clock; this.state = 'READY'; this.session = null; this.prepared = {}; this.loading = new Set(); this.errors = {}; this.debug = false; this.outer = new MockOuter(); this.last = clock(); this.deadline = null; this.feedback = null; this.events = []; }
  prepare(index) {
    if (!Number.isInteger(index) || !(0 <= index && index < 500)) throw new Error('测试题组应在0–499之间');
    if (index in this.prepared || this.loading.has(index)) return;
    this.loading.add(index);
    try {
      const rows = one_run(index), boards = [];
      for (const row of rows) {
        if (row.status !== 'ACCEPTED' || row.validationErrors.length) throw new Error('GENERATION_EXHAUSTED / QA rejected');
        const e = row.board, items = e.cargo.map(c => cargo(c.kind, c.instance)), ids = Object.fromEntries(items.map(c => [c.instance, c]));
        boards.push(new Board(e.batch, items, e.referenceSolution.left.map(i => ids[i]), e.referenceSolution.right.map(i => ids[i]), e.referencePhysicalBalanceRatio, e));
      }
      if (boards.length !== 3) throw new Error('GENERATION_EXHAUSTED');
      this.prepared[index] = boards;
    } catch (e) { this.errors[index] = String(e.message || e); } finally { this.loading.delete(index); }
  }
  sync() {
    const t = this.clock(), dt = Math.max(0, t - this.last); this.last = t; const s = this.session; if (!s) return;
    if (this.state === 'GAMEPLAY') { s.tick(dt); if (s.state === 'SETTLEMENT') { this.state = 'TIMEOUT'; this.deadline = t + .7; } }
    else if (this.state === 'EVALUATING' && t >= this.deadline) { const score = s.resolve_submission(); this.feedback = score.passed ? 'PASS' : 'NOT_PASS'; this.state = 'FEEDBACK'; this.deadline = t + .8; }
    else if (this.state === 'FEEDBACK' && t >= this.deadline) { if (s.state === 'TRANSITION') { this.state = 'TRANSITION'; this.deadline = t + .5; } else if (s.state === 'SETTLEMENT') { this.state = 'SETTLEMENT'; this.outer.apply(s.result); } else this.state = 'GAMEPLAY'; }
    else if (this.state === 'TRANSITION' && t >= this.deadline) { s.finish_transition(); this.state = 'GAMEPLAY'; this.feedback = null; }
    else if (this.state === 'TIMEOUT' && t >= this.deadline) { this.state = 'SETTLEMENT'; this.outer.apply(s.result); }
  }
  action(a) {
    this.sync(); const op = a.op, s = this.session;
    if (op === 'prepare') this.prepare(a.index);
    else if (op === 'debug') this.debug = Boolean(a.enabled);
    else if (op === 'how' && this.state === 'READY') this.state = 'HOW_TO_PLAY';
    else if (op === 'ready' && ['HOW_TO_PLAY', 'LIVELIHOOD_LIST'].includes(this.state)) this.state = 'READY';
    else if (op === 'livelihood' && this.state === 'CITY') this.state = 'LIVELIHOOD_LIST';
    else if (op === 'start' && this.state === 'READY') { const boards = this.prepared[a.index]; if (!boards) throw new Error(this.errors[a.index] || '题目尚未准备完成'); this.session = new Session(b => boards[b - 1], { mode: a.mode, timerConfig: a.seconds, phase: '晨', jobId: 'P1_STANDALONE', runId: uuid() }); this.state = 'GAMEPLAY'; this.feedback = null; }
    else if (['insert', 'remove', 'submit', 'abort'].includes(op) && this.state === 'GAMEPLAY') { if (op === 'insert') s.insert(a.id, a.side); else if (op === 'remove') s.remove(a.id, a.side); else if (op === 'abort') { s.request_abort(); this.state = 'ABORT_CONFIRM'; } else { s.submit(); this.state = 'EVALUATING'; this.deadline = this.clock() + .9; } }
    else if (op === 'cancel_abort' && this.state === 'ABORT_CONFIRM') { s.cancel_abort(); this.state = 'GAMEPLAY'; }
    else if (op === 'confirm_abort' && this.state === 'ABORT_CONFIRM') { s.confirm_abort(); this.outer.apply(s.result); this.state = 'LIVELIHOOD_LIST'; }
    else if (op === 'close' && ['READY', 'HOW_TO_PLAY'].includes(this.state)) this.state = 'LIVELIHOOD_LIST';
    else if (op === 'navigate' && this.state === 'SETTLEMENT') this.state = s.navigate(a.destination);
    else throw new Error('当前暂不可操作');
    this.last = this.clock(); this.events.push({ op, state: this.state, time: this.last }); return this.snapshot();
  }
  snapshot() {
    this.sync(); const s = this.session;
    const d = { state: this.state, debugEnabled: this.debug, prepared: Object.keys(this.prepared).map(Number), loading: [...this.loading], errors: this.errors, feedback: this.feedback };
    if (s) {
      const ev = evaluateLayout(s.left, s.right, s.board.reference_ratio, { debug: true }); const lw = s.left.reduce((x, c) => x + c.weight, 0), rw = s.right.reduce((x, c) => x + c.weight, 0), ratio = ev.physicalBalanceRatio;
      Object.assign(d, { lastTier: s.last_evaluation ? s.last_evaluation.performanceTier : null, batch: s.batch, mode: s.mode, remaining: s.remaining, left: s.left.map(c => c.instance), right: s.right.map(c => c.instance), slots: s.original_slots.slice(), waiting: [...s.waiting], canSubmit: this.state === 'GAMEPLAY' && s.can_submit,
        cargo: Object.fromEntries(s.board.items.map(c => [c.instance, { name: CATALOG[c.kind].name, kind: c.kind, weightLabel: CATALOG[c.kind].weightLabel, attributes: c.attributes.map(t => ATTR[t]) }])),
        balance: { ratio, direction: lw > rw ? -1 : rw > lw ? 1 : 0, band: ratio === null ? 'empty' : ratio >= .85 ? 'green' : ratio >= .80 ? 'yellow' : 'red' },
        warnings: Object.values(ev.penaltySources || {}).flat().filter(r => Object.entries(r.components).some(([k, v]) => v > 0 && ['heavyLayer', 'pressure', 'fragileHeavyAbove'].includes(k))).map(r => r.instance) });
      if (this.state === 'SETTLEMENT') { const r = s.result; d.settlement = { completed: r.primaryMetrics.completedBatchCount, tiers: s.results.map(x => x.performanceTier), base: r.payoutBreakdown.baseWage, extra: r.payoutBreakdown.extraWage, cash: s.mode === 'TRIAL' ? r.simulatedPayout : r.cashDelta }; }
      if (this.debug) { const dbg = s.debug(); Object.assign(dbg, { leftWeight: lw, rightWeight: rw, cargoWeights: Object.fromEntries(s.board.items.map(c => [c.instance, c.weight])), constraintWeight: s.board.metrics.constraintWeight, referenceBestPhysicalRatio: s.board.reference_ratio, result: s.result, mockOuter: this.outer.state, clockAdapter: 'monotonic operable seconds -> unchanged P0 tick (JS port)', testRunIndex: s.board.metrics.seed ?? null }); d.debug = dbg; }
    }
    return d;
  }
}
// Deployment parity probe (mirror of py/parity_probe.py): generator evidence + a scripted FORMAL session per index.

function fingerprint(indices) {
  const out = { python: 'js-port', runs: {} };
  for (const index of indices) {
    let t = 100.0; const clock = () => t; const g = new Game(clock); g.prepare(index); const boards = g.prepared[index];
    if (!boards) { out.runs[String(index)] = { error: g.errors[index] }; continue; }
    const row = { boards: [] };
    for (const b of boards) { const m = b.metrics; row.boards.push({ batch: b.batch, cargo: b.items.map(c => c.instance), reference: { left: b.left.map(c => c.instance), right: b.right.map(c => c.instance) }, referencePhysicalBalanceRatio: b.reference_ratio, referenceStackingScore: m.referenceStackingScore, referenceLayoutScore: m.referenceLayoutScore, allFeasibleLayoutCount: m.allFeasibleLayoutCount, nearOptimalLayoutCount: m.nearOptimalLayoutCount, distinctNearOptimalSolutions: m.distinctNearOptimalSolutions, bestFeasibleLayoutScore: m.bestFeasibleLayoutScore, constraintWeight: m.constraintWeight, weightPartitionAmbiguity: m.weightPartitionAmbiguity, boundary: m.boundary, warnings: m.warnings, qaStatus: m.qaStatus, generationAttempts: m.generationAttempts, decisionPatternSignature: m.decisionPatternSignature }); }
    g.action({ op: 'start', mode: 'FORMAL', seconds: 75, index });
    const place = pair => { [['left', pair[0]], ['right', pair[1]]].forEach(([side, items]) => { for (const it of items) g.action({ op: 'insert', id: it.instance, side }); }); };
    const settleCycle = () => { g.action({ op: 'submit' }); t += .91; g.snapshot(); t += .81; g.snapshot(); t += .51; g.snapshot(); };
    let s = g.session, b = s.board; place([b.left, b.right]); t += 3; settleCycle();
    s = g.session; b = s.board; let bad = null; for (const [a, z] of feasible_layouts(b.items)) { if (!evaluateLayout(a, z, b.reference_ratio).passed) { bad = [a, z]; break; } }
    if (bad) { place(bad); t += 2; g.action({ op: 'submit' }); t += .91; g.snapshot(); row.b2FirstFeedback = g.feedback; t += .81; g.snapshot(); for (const side of ['left', 'right']) for (const it of g.session[side].slice().reverse()) g.action({ op: 'remove', id: it.instance, side }); }
    place([b.left, b.right]); t += 2; settleCycle();
    s = g.session; b = s.board; place([b.left, b.right]); t += 4; settleCycle();
    g.snapshot(); const r = g.session.result;
    row.final = { state: g.state, remaining: g.session.remaining, completed: r.primaryMetrics.completedBatchCount, tiers: g.session.results.map(x => x.performanceTier), cashDelta: r.cashDelta, timeCostTicks: r.timeCostTicks, payout: r.payoutBreakdown, scores: r.secondaryMetrics, mockOuter: g.outer.state };
    out.runs[String(index)] = row;
  }
  return out;
}
function worstLayout(session) { if (!session) return null; const b = session.board; for (const [a, z] of feasible_layouts(b.items)) if (!evaluateLayout(a, z, b.reference_ratio).passed) return { left: a.map(c => c.instance), right: z.map(c => c.instance) }; return null; }

S.caravanEngine = Object.freeze({ CARGO_DATA, PROTOTYPE_CONFIG, PyRandom, CONFIG, F, T, CATALOG, ATTR, Cargo, cargo, cargoEquals, physical_ratio, classify, item_penalty, bag_summary, validate_layout, evaluateLayout, cmp, pyMin, canonical_layout, strategy_signature, serialize_signature, feasible_layouts, first_layer_qa, select_reference, partition_ambiguity, composition_qa, difficulty_qa, Board, analyze_board, generate_board, one_run, formal_entry, payout, Session, MockOuter, Game, fingerprint, worstLayout, sources: {"data.js":"78269080db8075e0","pyrandom.js":"cedfdc8fd6fbedfc","core.js":"14fc273339c6f93f","generator.js":"95f666d1f8a3921b","stress.js":"28f7e888a13bb1e3","session.js":"c0c47d324dce92cc","game.js":"329af8aaa90b9ee0"} });
})(globalThis.Silk = globalThis.Silk || {});
